---
- name: Converge
  hosts: all

  vars_files:
    - ../../vars/molecule_vars.yml

  vars:
    ansible_user: vagrant

  pre_tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      when: ansible_os_family == 'Debian'
      become: true

  roles:
    - role: pluggero.virtualbox
